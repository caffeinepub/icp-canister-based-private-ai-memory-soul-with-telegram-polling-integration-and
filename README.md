## ICP canister-based private AI memory (“soul”) with Telegram polling integration and web admin threads UI

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
